import { FC } from "react";
import { ProductFormatModel } from "../../models/ProductFormatModel";

const FormatItem: FC<ProductFormatModel> = (model: ProductFormatModel) => {
  return <div>FormatItem</div>;
};

export default FormatItem;
